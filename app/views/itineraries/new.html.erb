<div class="container-fluid">
  <div class= "mx-5">
    <div class="d-flex justify-content-between my-4">
      <div><%= @search.experience %></div>
      <div><%= @search.country %></div>
      <div><%= @search.budget %></div>
      <div><%= @search.days %></div>
    </div>
  </div>
  <div class="row my-2">
    <div class="col-7">
      <% @itineraries.reverse.each do |itinerary| %>
      <div class="mb-2">
        <div class="card main-card card<%= itinerary.id %>">
          <div class="card-body p-4">
            <div class="d-flex justify-content-between">
              <h5 class="card-title">Sex on the beach</h5>
              <div>
                <i class="fas fa-star"></i>
                <i class="fas fa-star"></i>
                <i class="fas fa-star"></i>
              </div>
            </div>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Diam sagittis accumsan ut posuere commodo. Enim mattis eget vestibulum bibendum ut ipsum at. Velit facilisis integer pulvinar faucibus et magnis ipsum, mattis. Mi neque porttitor volutpat ac luctus a augue.</p>
            <div class="d-flex justify-content-start h-25 mb-2"><span></span><p class="input"><%= itinerary.days %></p></div>
            <div class="d-flex justify-content-start h-25"><span></span><p class="input">$<%= itinerary.budget %></p></div>
            <div class= "d-flex justify-content-between align-items-center">
              <div>
                <%= link_to "", "#{bookmarks_path}?query=create&itinerary=#{itinerary.id}", method: :get, class: 'fas fa-plane-departure bookmark-icon' %>
              </div>
              <div align="right">
                <button class="button">
                <%= simple_form_for(@new_itinerary) do |f| %>
                  <%#= hidden_field_tag :id, value: itinerary.id %>
                  <%= f.hidden_field :id, value: itinerary.id %>
                  <%= f.hidden_field :experience, value: params[:search][:experience] %>
                  <%= f.hidden_field :country, value: params[:search][:country] %>
                  <%= f.hidden_field :budget, value: params[:search][:budget] %>
                  <%= f.hidden_field :days, value: params[:search][:days] %>
                  <%= f.submit "Build your adventure" %>
                <% end %>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
      <% end %>
    </div>
    <div class="col-5 mb-2">
      <%= render 'shared/itinerary-carousel' %>
    </div>
  </div>
</div>
